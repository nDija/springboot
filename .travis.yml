language: java
jdk:
  - oraclejdk9

before_install:
  - wget http://services.gradle.org/distributions/gradle-6.5-bin.zip
  - unzip -qq gradle-6.5-bin.zip
  - export GRADLE_HOME=$PWD/gradle-6.5
  - export PATH=$GRADLE_HOME/bin:$PATH
  - gradle -v